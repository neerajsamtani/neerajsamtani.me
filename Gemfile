# Only needed to preview the blog locally (`bundle exec jekyll serve`).
# GitHub Pages builds the site without it.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick"
